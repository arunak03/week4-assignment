doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Heroes
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", rel="stylesheet")
  body
    .container
    
      h3 This is pug template
      h1 Add a Hero
      form(action="#" method="post")
        label(for="title") Title:
        input#title.form-control(name="title", type="text", required)
        label(for="firstName") First Name:
        input#firstName.form-control(name="firstName", type="text", required)
        label(for="lastName") Last Name:
        input#lastName.form-control(name="lastName", type="text", required)
        label(for="power") Power:
        input#power.form-control(name="power", type="number", required)
        label(for="city") City:
        input#city.form-control(name="city", type="text", required)
        button.btn.btn-primary(type="submit") Add Hero

      hr

      h2 Heroes List
      table.table.table-bordered
        thead
          tr
            th Title
            th First Name
            th Last Name
            th Power
            th City
        tbody
          each hero in avengers
            tr
              td= hero.title
              td= hero.firstName
              td= hero.lastName
              td= hero.power
              td= hero.city
